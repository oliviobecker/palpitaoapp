namespace Palpitao.Api.Enums;

/// <summary>Approval lifecycle of a user's membership in a group.</summary>
public enum GroupUserStatus
{
    PendingApproval,
    Approved,
    Rejected,
    Inactive,
}
